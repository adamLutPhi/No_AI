package Classes;

/**
 * For there is no common term we could apply to the `mimes`
 * of `Sophron` and `Xenarchus` and the `Socratic dialogues`
 * on the one hand; and, on the other, to `poetic imitations`
 * in `iambic`, `elegiac`, or any `similar` metre.
 */

public class SocraticDialogue extends Dialogue {


}
